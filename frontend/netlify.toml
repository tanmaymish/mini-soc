[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Optional: If you ever add React Router (pages), this redirects all 404s to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
